version: "3.8"

services:
  fsku:
    build: .
    container_name: fsku_server
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
